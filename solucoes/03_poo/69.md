## Problema

69 -Considera a seguinte classe:

```cs
using System.Collections;
using System.Collections.Generic;

public class BasketballTeam : IEnumerable<string>
{
    public string Guard { get; set; }
    public string ShootingGuard { get; set; }
    public string SmallForward { get; set; }
    public string PowerForward { get; set; }
    public string Center { get; set; }

    public IEnumerator<string> GetEnumerator()
    {
        yield return Guard;
        yield return ShootingGuard;
        yield return SmallForward;
        yield return PowerForward;
        yield return Center;
    }

    IEnumerator IEnumerable.GetEnumerator()
    {
        return GetEnumerator();
    }
}
```
1.Cria uma classe Program com um método Main para testar a classe apresentada. Mais concretamente, no método Main deves:
	a) criar uma instância de BasketballTeam usando a sintaxe de inicialização de objectos com propriedades; e,
	b) imprimir no ecrã o nome de todos os elementos da equipa usando o facto da classe BasketballTeam ser iterável.

2.Supõe que o C# não tem a declaração yield return. Reescreve o método GetEnumerator tendo em conta essa limitação.

2.Quais são as vantagens óbvias do uso de yield return relativamente à forma como reescreveste o código na alínea anterior?

## Soluções

### Solução 1

1.

**Classe Program**
```cs
namespace ConsoleApp1
{
    class Program
    {
        static void Main(string[] args)
        {

            BasketballTeam yorks = new BasketballTeam("boris", "wallace", "gernab"
                , "james", "gobe");

            foreach(string s in yorks)
            {
                Console.WriteLine($"Elemento:{s}");
            }
        }
    }
}
```
2.

**Classe BasketballTeam**
```cs
using System.Collections;
using System.Collections.Generic;

public class BasketballTeam : IEnumerable<string>
{
    public string Guard { get; set; }
    public string ShootingGuard { get; set; }
    public string SmallForward { get; set; }
    public string PowerForward { get; set; }
    public string Center { get; set; }

    public BasketballTeam(string Guard, string ShootingGuard, string SmallForward,
        string PowerForward, string Center)
    {
        this.Guard = Guard;
        this.ShootingGuard = ShootingGuard;
        this.SmallForward = SmallForward;
        this.PowerForward = PowerForward;
        this.Center = Center;
    }
    
    public IEnumerator<string> GetEnumerator()
    {
        List<string> yorks = new List<string>
        {
            Guard,
            ShootingGuard,
            SmallForward,
            PowerForward,
            Center
        };
        return GetEnumerator();    
    }

    IEnumerator IEnumerable.GetEnumerator()
    {
        return GetEnumerator();
    }
}
´´´´
3. Com o uso da declaração *yield return* evita-se que haja um desperdício de recursos
	pois, este, permite devolver itens sem ser necessário criar uma coleção temporária.

*Por [Rodrigo Marques](https://github.com/RodrigoMarques23).*