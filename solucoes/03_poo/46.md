## Pergunta

46 - Indica o que é impresso no ecrã pelo seguinte código. Justifica a tua resposta.

```cs
static void Main(string[] args)
{
    Console.WriteLine(MetodoRecursivo(5));
}

static int MetodoRecursivo(int n)
{
    if (n == 0)
        return 0;
    else if (n % 2 == 0)
        return 3 + MetodoRecursivo(n - 1);
    else
        return 2 + MetodoRecursivo(n - 1);
}
```

## Soluções

### Solução 1

	O que é impresso no ecrã é o seguinte: "12". Isto porquê? Porque o metodo
	recursivo recebe um n = 5, e procede com o código onde neste caso corresponde
	à ultima condição(*else*) onde este devolve 2 + MetodoRecursivo(4), segue-se
	o MetodoRecursivo(4) que satisfaz a segunda condição(*else if*) em que retorna
	3 + MetodoRecursivo(3), e por aí adiante até chegar ao MetodoRecursivo(0).


*Por [Rodrigo Marques](https://github.com/RodrigoMarques23)*